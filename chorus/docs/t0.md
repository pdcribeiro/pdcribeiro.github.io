TASK T0 scaffold

GOAL
static mobile PWA shell ready

FILES
/ index.html
/ styles.css
/ app.js
/ manifest.json
/ sw.js

STEP 1 index.html
- basic HTML5
- meta viewport (width=device-width,initial-scale=1)
- link styles.css
- script app.js (defer)
- link manifest.json
- minimal body:
  div#app
    button#rec
    button#play
    button#export
    canvas#ring
    div#count

STEP 2 styles.css
- reset margin/padding
- full-screen flex column center
- large touch buttons
- simple dark/light colors
- canvas sized square

STEP 3 app.js
- register service worker (if 'serviceWorker' in navigator)
- wait for load → navigator.serviceWorker.register('sw.js')
- no app logic yet

STEP 4 manifest.json
- name, short_name
- display: standalone
- start_url: "."
- icons (placeholder 192,512)
- theme_color, background_color

STEP 5 sw.js
- cache name v1
- install → cache.addAll([index.html,styles.css,app.js,manifest.json])
- fetch → respondWith cache match fallback network

STEP 6 test
- open on mobile browser
- add to home screen
- offline reload works

DONE
